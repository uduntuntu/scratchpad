
# Name workspaces due display order
workspace $ws1 output eDP-1
workspace $ws2 output HDMI-2

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# scratchpad
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show

# mode "minimized"
set $miniscript ~/.local/bin/toggle-minimized.sh

mode "minimized" {
    bindsym Return exec --no-startup-id $miniscript; mode "default"
    bindsym Escape exec --no-startup-id $miniscript; mode "default"
    bindsym $mod+m exec --no-startup-id $miniscript; mode "default"
    bindsym $mod+l exec loginctl lock-session;
    bindsym --release $mod+Shift+s exec "scrot -shole --line style=dash,color='Dark Salmon',width=3,opacity=200 - | xclip -selection clipboard -target image/png && notify-send 'Screenshot copied to clipboard'"
}

bindsym $mod+m exec --no-startup-id $miniscript; mode "minimized"

###     Display configuration mode     ###
mode "Display" {
    # Laptop only (HDMI-2 off)
    bindsym l exec --no-startup-id xrandr \
        --output HDMI-2 --off \
        --output eDP-1 --mode 1920x1200 --pos 0x0 --primary; mode "default"

    # HDMI-2 left of eDP-1 (portrait side-by-side)
    bindsym h exec --no-startup-id xrandr \
        --output HDMI-2 --mode 1680x1050 --rotate left --pos 0x0 \
        --output eDP-1  --mode 1920x1200 --pos 1050x0 --primary; mode "default"

    # HDMI-2 above eDP-1 (landscape stacked)
    bindsym k exec --no-startup-id xrandr \
        --output HDMI-2 --mode 1680x1050 --rotate normal --pos 0x0 \
        --output eDP-1  --mode 1920x1200 --pos 0x1050 --primary; mode "default"

    # escape back to normal mode without changes
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# Enter display mode with Mod4+Shift+d
bindsym $mod+Shift+d mode "Display"

# Apply laptop-only layout by default when i3 starts
exec_always --no-startup-id xrandr \
  --output HDMI-2 --off \
  --output eDP-1 --mode 1920x1200 --pos 0x0 --primary
